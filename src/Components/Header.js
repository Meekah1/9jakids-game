import React from 'react';
import styled from 'styled-components';

const MainHead = styled.div`
  margin-top: 15px;
  max-width: 1600px;
  height: 40px;
  background-color: #fdc800;
`;

const Header = () => {
  return <MainHead>Header</MainHead>;
};

export default Header;
